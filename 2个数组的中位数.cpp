#include <stdio.h>
#include <iostream>
#include <vector>

using namespace std;


class Solution {
public:
    double findMedianSortedArrays(vector<int>& nums1, vector<int>& nums2) {
      double result;
      int nums1_index = nums1.size() / 2;
      int nums2_index = nums2.size() / 2;
      while (true) {
        if nums1[nums1_index]
      };
    }
};

int main() {
  Solution a;
  vector<int> nums1 = {1, 3};
  vector<int> nums2 = {2};
  cout << a.findMedianSortedArrays(nums1, nums2) << endl;
};

